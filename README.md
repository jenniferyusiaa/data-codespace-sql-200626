# restaurant_reviews

Tugas Managing SQL Data

Cara jalankan di Codespaces:

```bash
sudo service mysql start
sudo mysql -u root < 01_setup.sql
sudo mysql -u root < 02_insert.sql
sudo mysql -u root < 03_crud.sql
sudo mysql -u root < 04_queries.sql